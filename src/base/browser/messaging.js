import {NotImplementedException} from '../../core/exceptions';


export default class BrowserMessaging {
    addListener(callback) {
        throw new NotImplementedException();
    }

    send(message) {
        throw new NotImplementedException();
    }
}
